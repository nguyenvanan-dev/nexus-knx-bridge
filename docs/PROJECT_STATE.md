# READ THIS FIRST

**This document is the Single Source of Truth for the KNX Smart Home Platform.**

If this document conflicts with:
- `AGENTS.md`
- `IDENTITY.md`
- chat history
- AI memory

**THIS DOCUMENT WINS.**

Future AI sessions must always read this document first. If conflicts arise between this document and repository evidence, repository evidence wins and the inconsistency must be reported immediately.

---

# PROJECT_STATE.md (Version 3.0 - Frontend Integration)

## 0. Header
- **Document Version:** 3.0.0
- **Generated Date:** 2026-07-10
- **Generated By:** AI Lead Engineer
- **Repository:** `/home/an/knx-bridge`
- **Current Sprint:** Sprint E3.3 (Current)
- **Multi-Light Control Enablement**:
  - Dọn sạch rác `bulk_light_*` và `test_light*` khỏi `SQLite device registry` và `smarthome.db`.
  - Áp dụng Safety Gate: Backend API `/light` sử dụng allowlist cứng chỉ cho phép điều khiển physical lights (`den_led_day`, `den_tron`, `den_d`, `den_e`, `den_f`, `den_g`, `den_h`, `g1_den_tran`).
  - Frontend chỉ filter và render các thiết bị physical light (`type === 'light'` && `onoff_ga`).

#### Verification Evidence (Pending Physical Test)
| Device ID      | Type  | Curl Test (API Proxy) | Physical Result (Light ON/OFF) |
|----------------|-------|-----------------------|--------------------------------|
| `den_led_day`  | light |                       |                                |
| `den_tron`     | light |                       |                                |
| `den_d`        | light |                       |                                |
| `den_e`        | light |                       |                                |
| `den_f`        | light |                       |                                |
| `den_g`        | light |                       |                                |
| `den_h`        | light |                       |                                |
| `g1_den_tran`  | light |                       |                                |
| `room_all_on`  | scene | 403 Forbidden         | N/A (Blocked by Safety Gate)   |
| `ac_living`    | ac    | 403 Forbidden         | N/A (Blocked by Safety Gate)   |

## 1. Current Architecture
```text
Frontend Next.js
        |
Next.js API Proxy
        |
Python Backend
        |
OpenClaw CommandPipeline
        |
KNX Bus
```

## 2. Completed Milestones
- **Frontend responsive foundation (E1)**
- **Smart Home Dashboard UX redesign (E2)**
- **Backend/Frontend integration contract (E3A)**
- **Live device data integration (E3.1)**
- **Single device KNX control verification (E3.2)**

## 3. Verification Status

### Verified Physical Device:
- `den_led_day`
  - ON: **PASS**
  - OFF: **PASS**

### Known UNVERIFIED:
- Other light devices
- AC integration
- Curtain integration
- Scene execution
- Device feedback synchronization

## 4. Current Safety Limitation
- **Only `den_led_day` is allowed to send physical control commands.** (Hardcoded lock in `page.js`).

## 5. Next Objective: Sprint E3.3 - Multi-Light Control Enablement
The immediate next goal is to unlock all other Light devices for physical control, ensuring their KNX addresses are correctly mapped and responsive.

### Remaining Tasks for E3.3:
1. Audit all light devices in `SQLite device registry`
2. Verify KNX group address mapping
3. Remove temporary `den_led_day` safety lock
4. Test ON/OFF for every light device
5. Do not enable AC/Curtain/Scene yet

## 6. DO NOT REDISCOVER
Things already investigated and verified. **Do NOT spend time auditing these again unless requested.**
- ✓ **Backend API `/light`:** Used exclusively for triggering device states. `POST /devices/update` is for configuration only.
- ✓ **Next.js Proxy:** Successfully handling CORS and JWT forwarding.
- ✓ **Optimistic UI:** Rolls back seamlessly on 5000ms timeouts.
