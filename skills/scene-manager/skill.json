{
  "schema_version": 2,
  "name": "scene-manager",
  "description": "Quản lý kịch bản (Scene) của nhà thông minh. Dùng skill này để lưu kịch bản mới, xem thông tin một kịch bản, xem danh sách tất cả kịch bản, hoặc xóa kịch bản. Skill này KHÔNG kích hoạt kịch bản, nó chỉ thao tác với dữ liệu. Để kích hoạt, bạn hãy dùng skill này để đọc nội dung kịch bản, sau đó dùng skill knx-bridge để ra lệnh cho các thiết bị.",
  "intents": ["scene-manager"],
  "run": "python3 /home/an/knx-bridge/skills/official/scene-manager/main.py",
  "input": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": ["save", "get", "list", "delete"],
        "description": "Hành động: 'save' (lưu/cập nhật kịch bản), 'get' (lấy thông tin 1 kịch bản), 'list' (lấy danh sách tất cả kịch bản), 'delete' (xóa kịch bản)."
      },
      "scene_name": {
        "type": "string",
        "description": "Tên kịch bản (cần thiết cho save, get, delete). Ví dụ: 'Scene Chill'."
      },
      "scene_data": {
        "type": "string",
        "description": "Dữ liệu JSON của kịch bản (chỉ dùng cho 'save'). Ví dụ: '{\"description\": \"...\", \"actions\": [...]}'."
      }
    },
    "required": ["action"]
  }
}
